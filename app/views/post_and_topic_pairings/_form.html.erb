<%= form_for(@post_and_topic_pairing) do |f| %>
  <% if @post_and_topic_pairing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_and_topic_pairing.errors.count, "error") %> prohibited this post_and_topic_pairing from being saved:</h2>

      <ul>
      <% @post_and_topic_pairing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br />
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.number_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
