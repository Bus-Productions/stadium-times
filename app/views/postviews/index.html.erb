<h1>Listing postviews</h1>

<table>
  <tr>
    <th>User</th>
    <th>Post</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @postviews.each do |postview| %>
  <tr>
    <td><%= postview.user_id %></td>
    <td><%= postview.post_id %></td>
    <td><%= link_to 'Show', postview %></td>
    <td><%= link_to 'Edit', edit_postview_path(postview) %></td>
    <td><%= link_to 'Destroy', postview, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Postview', new_postview_path %>
