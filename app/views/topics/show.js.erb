<% @posts.each do |p| %>
	$('.topic_posts_pagination').append("<%= j render 'topic_post', :topic => p %>");
<% end %>

<% if @posts.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@posts) %>'); 
<% else %>
	$('.pagination').remove(); 
<% end %>
<% sleep 0.5 %>