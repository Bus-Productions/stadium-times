$('.topic_posts_pagination').append("<%= j render(@topic_posts)%>"); //this is the line that isnt working.
<% if @topic_posts.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@topic_posts) %>'); 
<% else %>
	$('.pagination').remove(); 
<% end %>
<% sleep 0.5 %>