$('.topic_show_pagination').append("<%= j render 'topic_post', :topic_posts => @topic_posts %>"); 
<% if @topic_posts.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@topic_posts) %>'); 
<% else %>
	$('.pagination').remove(); 
<% end %>
<% sleep 0.5 %>