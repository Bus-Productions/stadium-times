$('#new-comment-button').hide();
$('.new-comment-box').blur();
$('.new-comment-box').val('');

$('#comment_waiting_box').replaceWith("<%= j render 'posts/comment_detail', :comment => @saved_comment, :beta => (@saved_comment.comment_id) %>");
setCommentTextBehavior();