<%= form_for(@topic_follow) do |f| %>
  <% if @topic_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_follow.errors.count, "error") %> prohibited this topic_follow from being saved:</h2>

      <ul>
      <% @topic_follow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.number_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :manual_follow %><br />
    <%= f.check_box :manual_follow %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
